import type { Request, Response } from "express"

const timelineController = async (req: Request, res: Response) => {
    try {
        const index = req.params.index
        
    } catch (error) {
        
    }
}

export default timelineController