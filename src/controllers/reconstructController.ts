import type { Request, Response } from "express"

const reconstructController = async (req: Request, res: Response) => {
    try {
        // const repoId = 
        const fileName = "sample/d1/p1.txt"
        const index = 3


    } catch (error) {
        
    }
}

export default reconstructController